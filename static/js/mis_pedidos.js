/* Mis Pedidos - JS minimal */
document.addEventListener("DOMContentLoaded", function() {
    // Cargar datos de pedidos si es necesario
    const pedidosContainer = document.getElementById('pedidos-container');
    if (pedidosContainer) {
        // Script puede expandirse aquí para funcionalidades dinámicas
    }
});
