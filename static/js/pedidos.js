/* Pedidos - JS minimal */
document.addEventListener("DOMContentLoaded", function() {
    // Cargar historial de pedidos si es necesario
    const pedidosContainer = document.querySelector('.main-content');
    if (pedidosContainer) {
        // Script puede expandirse para funcionalidades dinámicas
    }
});
